import React from "react";
import { Button } from "antd";

const CustomButton = () => {
  return (
    <Button type="primary" block>
      Sign up or log in
    </Button>
  );
};

export default CustomButton;
